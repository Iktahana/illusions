name: Copilot Maintainer Loop

# DISABLED: GitHub CLI does not support automated custom agent invocation.
# Custom agents are used interactively via GitHub Copilot Chat:
#   @maintainer Issue #123 を修正してください
#
# Re-enable when GitHub adds workflow-triggered agent support.
on:
  workflow_dispatch: # Manual trigger only
