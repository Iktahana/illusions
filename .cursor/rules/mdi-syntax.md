# MDI Syntax Rule

When editing or generating `.mdi` content, follow the syntax rules defined in `MDI.md` at the repo root.

Key requirement:
- Headings must include anchor suffixes in the form `{#h<level>-<uuid>}`.
